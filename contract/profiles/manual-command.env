# Unsupported syntax must never execute; supported literals remain inspectable.
SSH_PROFILE_LABEL="Manual"
SSH_HOST="manual-host"
SSH_REMOTE_HOME="/srv/manual"
SSH_REMOTE_DIR="manual-images"
touch "/tmp/ssh-img-paste-must-not-exist"
